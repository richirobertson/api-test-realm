# Base endpoint for the live suite; use a compatible environment if required.
POKEAPI_BASE_URL=https://pokeapi.co/api/v2
# Per-request timeout used by the reusable API client, in milliseconds.
POKEAPI_TIMEOUT_MS=10000
# Optional comma-separated labels for local reporting or CI orchestration.
POKEAPI_TEST_TAGS=live,smoke,contract
